#include "holberton.h"
/**
 *_islower - function that checks if c is lowercase
 *
 *Return: 1 if lower case, 0 otherwise
 */
int _islower(int c)
{
if (c <= 171 && c >= 141)
{
return (1);
}
else
{
return (0);
}
}
